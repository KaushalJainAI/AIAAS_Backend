## Small diffs

Keep every change small and reviewable.

- One task, one concern. Do not bundle unrelated fixes into the same edit.
- Prefer editing the lines that need to change over rewriting the file.
- If a change grows past ~200 lines of diff, stop and say how you would split it.
- Never reformat a file you did not need to touch; formatting noise hides the real change.

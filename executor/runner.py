"""
DEPRECATED: Legacy Execution Runner.

This module contains the old 'NodeRunner' and 'WorkflowExecutor' classes
which have been superseded by 'ExecutionEngine' (engine.py) and 'WorkflowCompiler' (compiler.py)
using LangGraph.

Do not use this module for new code. 
It will be removed in a future cleanup.
"""
# Content removed to prevent accidental usage.
# If you need to reference the old logic, see git history.

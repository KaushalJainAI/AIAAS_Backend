"""
Where a turn's raw material comes from.

`attachments` is the file and RAG lifecycle behind an upload; `search` is the
web backend the `chat.tools.web` tools call. Both produce content the model
will see but neither decides what to do with it.
"""

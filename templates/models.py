"""
Emptied: the DAG template gallery was dropped in `0003_delete_template_gallery`.

The package survives only to carry that migration. Nothing should be added
here — agent templates are a `SubAgent` used as a starting point and need no
table of their own.
"""
